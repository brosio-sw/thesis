# Core deep-learning stack — PyTorch with CUDA 12.1 (RTX 4070 Ti)
# Install via: pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 \
#              --index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# HuggingFace ecosystem
transformers>=4.42.0
datasets>=2.18.0
accelerate>=0.30.0
tokenizers>=0.19.0
sentencepiece>=0.2.0

# Local toxicity classifier (no API key needed)
detoxify>=0.5.2

# Numerics / data
numpy>=1.26.0
pandas>=2.0.0
scipy>=1.12.0

# Visualisation
matplotlib>=3.8.0
seaborn>=0.13.0
tabulate>=0.9.0   # for DataFrame.to_markdown()

# Utilities
tqdm>=4.66.0
